import {Canvas, Meta, Story, ArgsTable} from "@storybook/addon-docs"
import {action} from "@storybook/addon-actions"
import {css, cx} from "@emotion/css"
import {useTimer} from "../src"

<Meta title="hooks/Use timer hook" />

export const Wrapper = ({defaultState}) => {
  const [secondsPassed, start, stop] = useTimer()
  return (
    <div
      className={css`
        padding: 1rem;
        background: #ccc;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        border-radius: 1rem;
        width: 25em;
      `}
    >
      <h1>Time passed: {secondsPassed.toFixed(3)}</h1>
      <button onClick={start}>Start</button>
      <button onClick={stop}>Stop</button>
    </div>
  )
}

<Canvas>
  <Story name="Use timer hook" args={{defaultState: false}}>
    {(args) => <Wrapper {...args} />}
  </Story>
</Canvas>

<ArgsTable story="Use timer hook" />
